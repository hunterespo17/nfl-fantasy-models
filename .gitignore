# Cached nflverse pulls, trained models, and generated outputs are all
# reproducible from code, so we don't commit them.
data/raw/
data/processed/
models/
outputs/

# BUT keep the hand-maintained inputs — the site needs these and they can't
# be re-pulled from nflverse.
!data/adp.csv
!data/win_totals.csv
!data/adp_history.csv
!data/playcallers.csv

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/
*.egg-info/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
