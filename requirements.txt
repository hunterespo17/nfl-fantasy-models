# Core data + modeling stack for the NFL fantasy project.
# Install with:  pip install -r requirements.txt
#
# nflreadpy is the officially maintained nflverse Python package.
# (The older nfl_data_py is deprecated -- do not use it.)

nflreadpy>=0.1.0      # nflverse data loader (pulls in polars + pyarrow)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3     # gradient boosting + metrics (no XGBoost/LightGBM needed)
matplotlib>=3.7       # charts
joblib>=1.3           # saving/loading trained models
pyarrow>=14           # fast parquet cache (usually installed via nflreadpy)
